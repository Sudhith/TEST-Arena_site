# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
jinja2==3.1.4
aiofiles==23.2.1
itsdangerous==2.2.0

# Database / ORM
sqlmodel==0.0.19
# SQLite is built-in to Python; swap DATABASE_URL for Postgres in production

# Auth / Security
bcrypt>=4.0.0

# CAPTCHA generation
captcha==0.6.0
pillow>=11.0.0   # 11.0+ required for Python 3.13 support

# Rate limiting
slowapi==0.1.9
limits==3.12.0

# Configuration
python-dotenv==1.0.1
pydantic-settings==2.3.4  # required by app/config.py (separate from pydantic)

# Dataset download (agent training + grid images)
# Used by scripts/download_dataset.py — not needed at runtime
datasets==2.20.0
